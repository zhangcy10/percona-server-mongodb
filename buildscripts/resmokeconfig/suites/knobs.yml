selector:
  js_test:
    roots:
    - jstests/knobs/knobs.js

executor:
  js_test:
    config:
      shell_options:
        readMode: commands
    fixture:
      class: MongoDFixture
      mongod_options:
        add_options:
          config: 'jstests/libs/config_files/knobs.json'
